// import { createRedsysAPI, PRODUCTION_URLS, SANDBOX_URLS } from 'redsys-easy'

// export const { createRedirectForm, processRestNotification } = createRedsysAPI({
//   secretKey: process.env.REDSYS_SECRET_KEY!,
//   urls: process.env.NODE_ENV === 'production' ? PRODUCTION_URLS : SANDBOX_URLS
// })
